---
description: 新增或修改代码时附加「父亲」标记注释，便于在工程中识别 Cursor 代写部分
alwaysApply: true
---

# 代码标记约定（Cursor → 父亲）

在本仓库中，**凡由 Cursor 新增或实质修改的代码块**，应在附近加上可检索的中文注释（单行或块注释均可），固定文案为：

**`这是我cursor给父亲写的`**

- Java / SQL / XML：使用 `//` 或 `/* */` 或 `<!-- -->`（XML）。
- Vue：`<template>` 内可用 `<!-- ... -->`；`<script>` 内用 `//` 或 `/* */`。
- `menu.js` 等单行 JSON 不易内嵌注释时，在**文件顶部**用 `//` 说明本次变更与上述标记的对应关系。

不要删改用户手写注释中已存在的其它说明，仅在本次变更范围内追加该标记。
